{
  "name": "vue-bin",
  "version": "1.0.0",
  "author": "joaqu1m",
  "scripts": {
    "start": "npm install && concurrently \"cd ./ui && npm install && npm run serve\" \"cd ./api && npm install && npm run dev\""
  },
  "dependencies": {
    "concurrently": "^8.2.0"
  }
}
